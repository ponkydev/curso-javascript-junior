/* -- Condiacionales: nos permite validar una porcion de codigo y hacer una accion dependiendo su resultado.

if: se utiliza para la primera validacion. */
if (true && false) {
    /* Si el resultado es true, realiza un accion, sino, pasa a la siguiente condicion*/
} /* Los bloques de codigo van dentro de llaves {}.

else if: se utiliza si la condicion anterior fue falsa. */
else if (false) {
    /* Si el resultado es true, realiza un accion, sino, pasa a la siguiente condicion*/
}
/* se pueden utilizar tantos else if como condiciones existan */
else if (false) {
    /* Si el resultado es true, realiza un accion, sino, pasa a la siguiente condicion */
}
/*
else: se utiliza para finalizar si ninguna de las condiciones se cumplio. */
else {
    /* Realiza una accion */
}