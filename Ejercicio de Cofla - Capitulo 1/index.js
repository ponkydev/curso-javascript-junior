dineroCofla = prompt("Cuanto dinero tienes Cofla?");
dineroRoberto = prompt("Cuanto dinero tienes Roberto?");
dineroPedro = prompt("Cuanto dinero tienes Pedro?");

if (dineroCofla >= .6 && dineroCofla < 1) {
    alert("Cofla comprate el helado de agua. Tu devuelta sera de " + (dineroCofla - .6));
}
else if (dineroCofla >= 1 && dineroCofla < 1.6) {
    alert("Cofla comprate el helado de crema. Tu devuelta sera de " + (dineroCofla - 1));
}
else if (dineroCofla >= 1.6 && dineroCofla < 1.7) {
    alert("Cofla comprate el heladix. Tu devuelta sera de " + (dineroCofla - 1.6));
}
else if (dineroCofla >= 1.7 && dineroCofla < 1.8) {
    alert("Cofla comprate el heladovich. Tu devuelta sera de " + (dineroCofla - 1.7));
}
else if (dineroCofla >= 1.8 && dineroCofla < 2.9) {
    alert("Cofla comprate el helardo. Tu devuelta sera de " + (dineroCofla - 1.8));
}
else if (dineroCofla >= 2.9) {
    alert("Cofla comprate un potecito de helado con confites o el de 1/4 KG. Tu devuelta sera de " + (dineroCofla - 2.9));
}
else {
    alert("Cofla no tienes dinero suficiente.");
}

if (dineroRoberto >= .6 && dineroRoberto < 1) {
    alert("Roberto comprate el helado de agua. Tu devuelta sera de " + (dineroRoberto - .6));
}
else if (dineroRoberto >= 1 && dineroRoberto < 1.6) {
    alert("Roberto comprate el helado de crema. Tu devuelta sera de " + (dineroRoberto - 1));
}
else if (dineroRoberto >= 1.6 && dineroRoberto < 1.7) {
    alert("Roberto comprate el heladix. Tu devuelta sera de " + (dineroRoberto - 1.6));
}
else if (dineroRoberto >= 1.7 && dineroRoberto < 1.8) {
    alert("Roberto comprate el heladovich. Tu devuelta sera de " + (dineroRoberto - 1.7));
}
else if (dineroRoberto >= 1.8 && dineroRoberto < 2.9) {
    alert("Roberto comprate el helardo. Tu devuelta sera de " + (dineroRoberto - 1.8));
}
else if (dineroRoberto >= 2.9) {
    alert("Roberto comprate un potecito de helado con confites o el de 1/4 KG. Tu devuelta sera de " + (dineroRoberto - 2.9));
}
else {
    "Roberto no tienes dinero suficiente.";
}

if (dineroPedro >= .6 && dineroPedro < 1) {
    alert("Pedro comprate el helado de agua. Tu devuelta sera de " + (dineroPedro - .6));
}
else if (dineroPedro >= 1 && dineroPedro < 1.6) {
    alert("Pedro comprate el helado de crema. Tu devuelta sera de " + (dineroPedro - 1));
}
else if (dineroPedro >= 1.6 && dineroPedro < 1.7) {
    alert("Pedro comprate el heladix. Tu devuelta sera de " + (dineroPedro - 1.6));
}
else if (dineroPedro >= 1.7 && dineroPedro < 1.8) {
    alert("Pedro comprate el heladovich. Tu devuelta sera de " + (dineroPedro - 1.7));
}
else if (dineroPedro >= 1.8 && dineroPedro < 2.9) {
    alert("Pedro comprate el helardo. Tu devuelta sera de " + (dineroPedro - 1.8));
}
else if (dineroPedro >= 2.9) {
    alert("Pedro comprate un potecito de helado con confites o el de 1/4 KG. Tu devuelta sera de " + (dineroPedro - 2.9));
}
else {
    "Pedro no tienes dinero suficiente.";
}